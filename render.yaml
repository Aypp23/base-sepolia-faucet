services:
  - type: web
    name: base-sepolia-faucet-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: RPC_URL
        value: https://sepolia.base.org
      - key: FAUCET_AMOUNT
        value: 0.001
      - key: RATE_LIMIT_HOURS
        value: 24
      - key: DB_PATH
        value: ./faucet.db 